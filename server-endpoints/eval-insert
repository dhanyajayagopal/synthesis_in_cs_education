#!/usr/bin/env python3

import runner
import common

def run_function(testInput, insert):
    return common.node_to_js(
        insert(common.node_from_js(testInput[0]), int(testInput[1])),
        "insert"
    )

runner.run(run_function, "insert", "tree")
