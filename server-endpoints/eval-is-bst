#!/usr/bin/env python3

import runner
import common

def run_function(testInput, is_bst):
    return common.bool_to_js(
        is_bst(common.node_from_js(testInput)),
        "is_bst"
    )

runner.run(run_function, "is_bst", "code")
