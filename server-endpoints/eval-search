#!/usr/bin/env python3

import runner
import common

def run_function(testInput, search):
    return common.bool_to_js(
        search(common.node_from_js(testInput[0]), int(testInput[1])),
        "search"
    )

runner.run(run_function, "search", "code")
